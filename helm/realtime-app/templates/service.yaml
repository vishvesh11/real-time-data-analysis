apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-app.fullname" . }}
  labels:
    {{- include "realtime-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "realtime-app.selectorLabels" . | nindent 4 }}
