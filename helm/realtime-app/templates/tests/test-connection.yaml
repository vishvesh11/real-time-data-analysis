apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "realtime-app.fullname" . }}-test-connection"
  labels:
    {{- include "realtime-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "realtime-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
